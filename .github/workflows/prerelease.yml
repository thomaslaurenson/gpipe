name: Prerelease

on:
  workflow_call

permissions:
  contents: write

jobs:
  prerelease:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
          fetch-tags: true

      - uses: actions/setup-go@v7
        with:
          go-version-file: go.mod
          cache: true

      # Three outcomes, three behaviours: the release exists, so delete it; gh
      # reports it missing, so carry on; gh failed for any other reason, so
      # stop. Collapsing the third into the second would let a rate limit or an
      # auth failure look like "no release exists", and the job would then
      # publish on top of a release whose state it never established.
      #
      # `2>&1 >/dev/null` captures stderr while discarding stdout, and the order
      # matters: redirections apply left to right, so stderr is pointed at the
      # still-open capture before stdout goes to /dev/null.
      - name: Delete any existing dev release
        run: |
          if err=$(gh release view "dev" 2>&1 >/dev/null); then
            gh release delete "dev" --yes --cleanup-tag
          elif grep -qi "release not found" <<<"$err"; then
            echo "No existing dev release"
          else
            echo "::error::could not determine whether a dev release exists: ${err}"
            exit 1
          fi
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Tag and push dev tag
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git tag -f dev
          git push origin dev --force

      - name: Build binaries
        uses: goreleaser/goreleaser-action@v7
        with:
          version: "~> v2"
          args: build --snapshot --clean --config .goreleaser.prerelease.yml

      - name: Create dev release
        run: |
          gh release create "dev" \
            --title "Dev (Pre-release)" \
            --notes "Built from commit [${{ github.sha }}](${{ github.server_url }}/${{ github.repository }}/commit/${{ github.sha }})" \
            --prerelease \
            dist/gpipe-*
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
